# ===== Secret MongoDB =====
# Kubernetes va encoder ces données en base64 automatiquement
apiVersion: v1
kind: Secret
metadata:
  name: mongo-secret
type: Opaque
stringData:
  MONGO_INITDB_ROOT_USERNAME: admin
  MONGO_INITDB_ROOT_PASSWORD: supermotdepasse123
