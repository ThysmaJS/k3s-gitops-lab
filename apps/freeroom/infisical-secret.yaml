apiVersion: secrets.infisical.com/v1beta1
kind: InfisicalStaticSecret
metadata:
  name: ghcr-pull-secret
  namespace: freeroom
spec:
  infisicalAuthRef:
    name: machine-identity
    namespace: infisical
  sources:
    - projectSlug: k3s-gitops-lab
      environmentSlug: prod
      secretPath: /freeroom/ghcr-pull-secret
  syncOptions:
    refreshInterval: 5m
  targets:
    - kind: Secret
      name: ghcr-pull-secret
      namespace: freeroom
      creationPolicy: Owner
      secretType: kubernetes.io/dockerconfigjson
      template:
        engineVersion: v1
        data:
          .dockerconfigjson: "{{ .dockerconfigjson.Value }}"
---
apiVersion: secrets.infisical.com/v1beta1
kind: InfisicalStaticSecret
metadata:
  name: freeroom-secrets
  namespace: freeroom
spec:
  infisicalAuthRef:
    name: machine-identity
    namespace: infisical
  sources:
    - projectSlug: k3s-gitops-lab
      environmentSlug: prod
      secretPath: /freeroom/freeroom-secrets
  syncOptions:
    refreshInterval: 5m
  targets:
    - kind: Secret
      name: freeroom-secrets
      namespace: freeroom
      creationPolicy: Owner
      secretType: Opaque
